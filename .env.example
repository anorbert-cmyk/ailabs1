# Perplexity API key (server-side only, never exposed to client)
PERPLEXITY_API_KEY=

# Anthropic API key for Haiku classifier (optional — improves section classification)
# WARNING: In development, this is exposed to the browser via VITE_ prefix.
# For production, use a server-side proxy instead.
VITE_ANTHROPIC_API_KEY=

# Set to 'true' to enable API calls via the dev proxy
VITE_API_ENABLED=true
